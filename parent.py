#!/usr/bin/python3
import os
import sys
import random

flag = 1
for i in range(n) :
      if child == 0 :
              break
      child = os.fork
if flag > 0 :
      for i in range(0, n)
              os.wait()
              print("Child number ", i, "finished")
else :
       os.execl('child.py', "Executiom child.py", str(random.choice(range(5, 11))))
